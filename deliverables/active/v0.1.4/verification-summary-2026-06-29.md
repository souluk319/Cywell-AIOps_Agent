# v0.1.4 Verification Summary - 2026-06-29

## Scope

This summary captures the current v0.1.4 branch verification state for the Cywell PBS/customer-data/RAG/LLM Wiki/topology integration.

## Latest Update - 2026-06-30

The real PBS runtime overlay now boots in CRC under namespace `playbookstudio` and satisfies the Cywell runtime contract.

| Command | Result | Notes |
| --- | --- | --- |
| `npm run deploy:pbs:runtime:crc` | PASS, 96 checks | Deploys the approved PBS source SHA `6604777abb9e6bd44a83c6a12f36e31ac396489e`, builds or reuses only a local PBS runtime image carrying `org.opencontainers.image.revision` for that SHA, grants only the needed `anyuid` SCC to `playbookstudio` and `terminal-broker`, replays PBS one-shot migration/seed jobs after Postgres readiness, verifies each migration/seed Job ran the source-bound local image digest, applies the graph foundation migration, mounts the pinned source overlay, records CRC source-overlay hotfix `wiki-loop-crc-compat-20260630`, routes CRC embeddings to an in-cluster deterministic stub, verifies HTTPS readiness, Service endpoints, ready pod SCC, ready runtime pod source-image digest, DB/pgvector/index parity with zero missing/stale embedding entries, `official_docs` and `study_docs` corpus counts/scopes, and compiled Wiki readiness. Evidence: `test-results/cas-pbs-runtime-crc-deployment.json` |
| `$env:CAS_PBS_SOURCE_HEAD="6604777abb9e6bd44a83c6a12f36e31ac396489e"; $env:CAS_PBS_SOURCE_DIR="F:\AI_Projects\PBS-Dev3-cywell-v014-source-pin-clone"; npm run verify:release:source-pinning` | PASS, 62 checks | Refreshes strict pinned PBS source evidence and proves the approved remote ref still contains the pinned SHA. Evidence: `test-results/cas-pbs-source-contract-pinned.json` |
| `npm run verify:pbs:preflight:live:site:preapply` | FAIL expected, 51 PASS / 11 FAIL | Generated site render, source pin evidence, PBS namespace/service/endpoint/source revision, and Service-DNS runtime health now pass. The runtime health probe verifies the OpenShift service-ca signed PBS serving cert using `/var/run/secrets/kubernetes.io/serviceaccount/service-ca.crt`. Remaining failures are external cutover prerequisites: `cas-pbs-auth`, `cas-knowledge-postgres-live`, real live prereq render/hash binding, fresh release image evidence after a clean committed HEAD, release ImageStreamTag digest binding to that fresh evidence, and pruning the legacy dev `cas-knowledge-postgres` Secret before pbs-live cutover. |

## Passing Verification Gates

| Command | Result | Notes |
| --- | --- | --- |
| `npm run verify` | PASS | Full local gate including contracts, gateway, knowledge engine, brain, OpenShift evidence, console build, browser launcher DOM, browser topology DOM, console integration, CRC connection preview, required PBS source contract against clean clone `F:\AI_Projects\PBS-Dev3-cywell-v014-source-pin-clone`, PBS live prerequisite renderer self-test, PBS cutover bundle renderer self-test, and manifest verification |
| `npm run verify:knowledge-engine` | PASS, 107 checks | Includes Gateway owner verification, signed internal owner headers, recursive customer/workspace/tenant scope mismatch rejection, nested scope alias scrubbing before PBS proxying, strict ConfigMap-driven customer workspace ACL behavior, explicit customer ID requirement, invalid ACL fail-closed behavior even when enforcement is unset, customer mismatch not-proxied fail-closed coverage, internal bearer/header stripping, public health/capabilities sanitization, unsafe upload/URL ingest rejection, private ingest lane enforcement before local indexing or PBS outbound calls, PBS-style upload report `items` with `viewer_path`, `graph_summary`, and `chunk_previews`, local Wiki Vault graph alias/context viewer lineage, selected-upload source scoping for RAG, RAG scope allowlisting to `user_upload` and `wiki_vault`, PBS-style LLM Wiki staged run/status and note overlay metadata, RAG citations from vault-only context, PBS shadow/live adapter contracts, PBS live request alias rejection before outbound calls, PBS live response row-level scope proof, decoy proof rejection, rich PBS topology signal preservation, topology provenance, orphan endpoint topology normalization, and single-candidate PBS graph normalization |
| `npm run verify:console:launcher-dom:built` | PASS, 11 checks | Browser-required smoke mounts the actual `CASContextProvider` plus `useCASLauncher`, rejects modal-mounted launcher regressions, verifies exactly one fixed bottom-right CAS launcher button, suppresses pre-existing and late-inserted OpenShift Lightspeed launcher variants, prevents duplicates after rerender/remount, opens the panel, calls `/api/aiops/brainz`, posts read-only `/api/aiops/query` bodies, preserves conversation state, and reports no browser errors |
| `npm run verify:console:topology-dom:built` | PASS, 53 checks | Browser-required smoke with corpus reports, upload indexing, URL ingest, selected-document/full-corpus RAG, Full corpus URL unpinning plus reload persistence, invalid `document_id` deep-link repair before RAG, invalid `document_id` clearing when reports are empty, knowledge-menu query preservation, customer-change stale selected-document scope clearing, late upload/RAG/topology response stale-data protection, Wiki-note deep links proving `document_id` is used by LLM Wiki requests, browser back/forward route-state reconciliation, rendered RAG citations, Wiki Loop stages, selected-document note lineage, topology auto-load, empty/error reload stale-data protection, PBS-rich KPI/tone/signal-leader/link-filter rendering, PBS-rich signal-node RAG scope from selected context/upload lineage, Wiki Vault side-channel token/context/relation panels, filter-scoped node index, corpus/viewer deep links from PBS-rich topology nodes and context rows, orphan edge fallback nodes, mixed-scope graph selection, dense 28-node searchable index, and 1024px/390px overflow/overlap checks |
| `npm run verify:console:integration:built` | PASS, 121 checks | Includes static app `innerHTML` rejection, structural `/cywell/topology` manifest routing/navigation, provider-rendered launcher proof, modal launcher rejection, native-Lightspeed suppression, topology auto-load, topology-selected source-scoped RAG request checks, Customer Corpus Workbench/viewer route checks, explicit all-corpus vs selected-document scope controls, Full corpus query-pin cleanup, URL ingest corpus joining for PBS `pages`/`upload_results`, selected-corpus RAG and selected-document wiki-loop/note request checks, Wiki-note document-lineage deep links, invalid query-document repair/empty-report clear guards, upload-report repair without clobbering topology/wiki viewer lineage, stale async customer/document scope guards, popstate URL reconciliation, PBS topology signal fields/counts, Signal leaders, Wiki Vault side-channel panels, and built plugin route/bundle checks |
| `npm run verify:pbs:source-contract:required` | PASS, 60 total checks: 60 PASS / 0 WARN | Verifies the clean `F:\AI_Projects\PBS-Dev3-cywell-v014-source-pin-clone` Docker/compose runtime contract, approved remote `https://github.com/souluk319/PBS_DEV_Part3.git`, port `8765`, `/api/health`, upload, URL ingest, chat/RAG, public chat gateway controls, Wiki Vault, Wiki Loop, owner-scoped reports, selected uploads, graph, chunk-preview API surface, Qdrant-free OpenShift core contract, and live source-stamp runbook against the Cywell PBS runtime contract. Required evidence is written separately from optional evidence; strict release source pinning writes `test-results/cas-pbs-source-contract-pinned.json` |
| `$env:CAS_PBS_SOURCE_HEAD="6604777abb9e6bd44a83c6a12f36e31ac396489e"; $env:CAS_PBS_SOURCE_DIR="F:\AI_Projects\PBS-Dev3-cywell-v014-source-pin-clone"; npm run verify:release:source-pinning` | PASS, 62 checks | Strict source pin evidence is now written to `test-results/cas-pbs-source-contract-pinned.json` from clean PBS branch `kugnus/cywell-v0.1.4-source-contract` on approved remote `https://github.com/souluk319/PBS_DEV_Part3.git`; the fetched approved remote ref `origin/kugnus/cywell-v0.1.4-source-contract` contains the pinned full SHA and the exact PBS contract file hash set |
| `npm run verify:pbs:live-prereqs` | PASS, 15 checks | Self-tests live prerequisite rendering, repo-local and repo-root raw Secret output rejection, generated `pbs-live-site` overlay rendering with `oc`/`kubectl` fallback, redacted summary output, owner-HMAC Secret validation, wildcard/default/string wildcard/string-grant/broad group/non-string customer ACL rejection, Postgres database URL/Secret mismatch rejection, supported env-var error naming, non-secret input template generation, and current-head redacted input-validation evidence |
| `npm run verify:pbs:cutover-bundle` | PASS, 25 self-test checks | Self-tests blocker extraction, artifact hashing, redaction, live-ready cluster cutover smoke evidence requirement, live-prereq hash drift/path escape rejection, generated-site preapply hash binding/freshness, live runtime source revision binding, cluster-identity mismatch rejection, dirty local evidence rejection, dirty/unpinned PBS source rejection, exact PBS source contract hash-set validation, approved PBS and Cywell remote fetch/ref proof rejection, stale/future source proof rejection, prereq self-test evidence rejection, missing prereq hash rejection, live-prereq output directory binding, and external blocker retention in active blockers when local evidence is invalid |
| `npm run verify:deploy:manifests` | PASS, 309 checks | Includes base/shadow/live/CRC render checks, HMAC Secret refs, no tracked dev DB Secret, generated pbs-live site preflight scripts, strict Gateway customer ACL checks, pbs-live Gateway customer ACL config, pbs-live Postgres release image pinning, PBS source-contract static coverage with split optional/required/pinned evidence, approved remote identity, and approved remote ref containment, private source-lane policy coverage, PBS-compatible local schema checks, PBS-style local upload report/RAG source-scope checks, LLM Wiki staged contract checks, release image promotion force/evidence binding coverage, explicit strict shadow preflight script coverage, HTTPS PBS service-token transport, OpenShift service-ca trusted PBS runtime probing, PBS live response scope guard coverage, structural Kubernetes API egress matching, PBS runtime service-port/source-revision/endpoint health readiness checks, PBS `/api/health` + `/api/wiki-loop/status` normalization, live Secret hash binding to prereq evidence, Secret content/applied NetworkPolicy union self-checks, CRC/release source and cluster identity binding, exact live lineage smoke self-checks, owner-HMAC live prereq/preflight checks, secure live prereq output/argv checks, cutover bundle strict clean-source/real-render/Cywell-source/cluster-smoke evidence checks, launcher DOM harness coverage, direct console script build prerequisites, and CRC console-operator plugin replacement for native Lightspeed plus legacy OpsLens removal |

Current hardening additions after the initial summary:

- Tracked base manifests no longer contain a literal dev Postgres password or database URL.
- `deploy:crc` creates local-only random dev Postgres and internal owner-HMAC Secrets when missing.
- Gateway signs derived owner headers; Knowledge Engine rejects unsigned owner headers when HMAC is configured.
- Gateway and Knowledge Engine fail closed if the internal owner-HMAC Secret is missing.
- Public Gateway `/healthz`, `/api/aiops/healthz`, `/api/knowledge/healthz`, and `/api/knowledge/capabilities` omit provider and owner identity internals.
- PBS live preflight checks the live `DATABASE_URL` over TCP through the Postgres service.
- Base manifests verify OpenShift API TLS with the mounted namespace CA and verify Lightspeed TLS with the mounted OpenShift service CA instead of disabling TLS verification.
- CRC-specific Kubernetes API egress, lab Lightspeed ingress, and lab-only Lightspeed TLS insecure override are owned by the CRC overlay, not the base render.
- `release:crc:v0.1.4` promotes CRC `:dev` app images plus the running Postgres digest into `v0.1.4` ImageStreamTags; moving an existing release tag now requires `CAS_RELEASE_FORCE=true`.
- pbs-live Postgres now renders as `image-registry.openshift-image-registry.svc:5000/cywell-ai-sentinel/cas-knowledge-postgres:v0.1.4`.
- Topology UI now ignores late customer topology responses, selects one graph payload scope instead of mixing wrapper/nested graph lists, reconciles visible KPI counts after fallback endpoint creation, and announces empty/error states to assistive tech.
- PBS preflight now fails unsupported overlay names, exposes `verify:pbs:preflight:shadow`, validates applied NetworkPolicies structurally, requires live `DATABASE_URL` to target the Postgres Service on port 5432, and checks release ImageStreamTag resolution.
- Local `verify:pbs:cutover` now requires `CAS_PBS_BASE_URL` plus PBS bearer token material; cluster cutover readiness remains gated by `verify:pbs:cutover:cluster`.
- Knowledge Engine now rejects unsafe upload extensions/MIME, invalid base64, oversized decoded uploads, oversized OOXML zip payloads, and loopback/private URL ingest before indexing.
- PBS-live upload policy now runs before outbound PBS calls, and verifier asserts rejected live uploads create no PBS request.
- PBS-live responses are checked against the requested customer ID and owner/PBS user hash before CAS exposes report rows, wiki vault payloads, or topology graphs.
- PBS service-token auth now fails closed without token material and rejects non-local plain HTTP transport; rendered pbs-shadow/live default to HTTPS.
- Gateway can require customer workspace ACL before private knowledge proxying; live mode reads `CAS_KNOWLEDGE_CUSTOMER_ACCESS_JSON` from `cas-knowledge-live-config/customer-access-json`, rejects unmapped customers with `403`, rejects missing customer IDs with `400`, rejects invalid configured ACLs with `503`, and rejects conflicting nested `source_metadata.customer_id` with `400`.
- Gateway now recursively scans query/body customer, workspace, tenant, and customer-workspace aliases, rejects nested scope smuggling before PBS, strips nested scope aliases from forwarded payloads, and preserves only the verified canonical `customer_id`.
- Knowledge Engine PBS response scope checks now include customer, workspace, tenant, and customer-workspace aliases; fake-live verification blocks a cross-scope RAG citation leak with `pbs-scope-mismatch`.
- Strict live preflight checks the live customer ACL env wiring through the generated site overlay, rejects default/wildcard/string/broad-principal ACLs, confirms applied live ConfigMap values, requires release evidence sourced from current non-stale CRC deployment evidence, compares ready pod digests to promoted release evidence, and confirms all applied ingress/egress NetworkPolicies selecting knowledge-engine pods stay scoped.
- PBS live release smoke no longer permits read-only exception bypass for cutover/release checks, and direct-engine block verification requires an actual in-cluster blocked result instead of accepting any non-200 response.
- Preflight and live-smoke evidence artifacts are mode-specific, so shadow, live preapply, live applied, local cutover, and cluster cutover runs do not overwrite each other.
- Local Postgres now creates PBS-compatible `tenants`, `workspaces`, `document_sources`, `parsed_documents`, `document_chunks`, `chunk_embeddings`, and graph tables; CAS ingest writes PBS-compatible document/chunk shadow rows, deterministic local hash embeddings, and customer-scoped graph entity/mention/relation rows. These rows prove CRC schema/lineage compatibility, not PBS production embedding model parity.
- Local upload reports and Wiki Vault selected uploads now expose PBS-style `items`, `document_source_id`, readiness flags, `graph_summary`, top-level `graph`, context `viewer_path`, source scopes, and `chunk_previews`, so topology/RAG consumers can use the same report shape without a live PBS backend.
- Topology inspector RAG actions now send selected topology document scope into `/api/knowledge/rag/query`; local RAG honors `active_document_id`, `enabled_upload_document_ids`, `enabled_source_scopes`, and `restrict_uploaded_sources`, and citations include `document_source_id`, `viewer_path`, `source_scope`, and `source_collection`.
- Customer Corpus Workbench now renders uploaded document/report cards, chunk previews, graph summaries, selected document state, viewer deep links, selected-corpus RAG, and selected-document wiki-loop actions under the Cywell customer data route.
- Knowledge route scope is explicit: operators can switch between full private corpus and selected-document mode, deep-linked documents auto-load reports, URL ingest can join the corpus from PBS `pages` and `upload_results`, saved wiki notes refresh against the current selected document, and result/error text remains visible outside the debug JSON panel.
- User upload and URL ingest payloads are forced into the private lane (`source_scope=user_upload`, `visibility=private_user`) before local storage or PBS outbound requests; RAG source selection is allowlisted to private `user_upload` and `wiki_vault` scopes.
- Private ingest strips both `source_metadata` and camelCase `sourceMetadata` reserved lane, owner, customer, tenant, workspace, and source-collection aliases before PBS outbound calls; PBS-live RAG fails closed for object citations without customer/owner proof and primitive string/source citation leaks.
- Local LLM Wiki loop now returns PBS-style `run_id`, `stages`, `duration_ms`, `warnings`, `summary`, `last_run`, and `compiled_wiki_status`; manual Wiki Vault notes preserve `overlay_id`, `book_slug`, `note_type`, target refs, wikilinks, and tags.
- PBS Wiki Vault topology normalization now preserves PBS summary aliases, wikilinks, tags, entity/concept nodes, degree/weight, source/viewer fields, selected context/uploads, and relation signals.
- The Cywell topology dashboard renders PBS-rich graph semantics as KPIs, node tones, type filters, Signal leaders, Wiki Vault token/context/relation side-channel panels, inspector metadata, relation lines, and node-to-RAG actions in the browser-backed release gate.
- The Cywell console plugin owns the topology visualization design for v0.1.4; no external dashboard design file is required. The current design includes graph canvas, KPI strip, type filters, signal leaders, relation grid, inspector, viewer links, and corpus-to-RAG/wiki actions.
- CRC release promotion is now bound to current-HEAD `test-results/cas-crc-deployment.json`; app release sources must match verified runtime digest evidence before `v0.1.4` tags move, release evidence records `sourceEvidenceHead` and rejects stale-source promotion in strict preflight, Postgres records both the verified external runtime digest and promoted internal ImageStream digest, and strict live preflight compares applied release tags to `test-results/cas-release-images.json`.
- Strict live preflight now checks Gateway Kubernetes API egress structurally, requiring the API target IP and port to be allowed by the same NetworkPolicy egress rule.
- Local Wiki Vault now extracts `[[wikilinks]]`, `#tags`, URLs, concepts, relations, backlinks, selected context/uploads, and can feed RAG citations from wiki-only context.
- Strict live preflight now checks `playbookstudio-runtime` Service DNS/port `8765`, ready Endpoints on port `8765`, ready pod labels, ready pod source-revision stamps against the approved full PBS SHA, and `/api/health` through the Service DNS when the runtime Service exists; it also rejects placeholder/short `cas-pbs-auth` bearer tokens, validates decoded `cas-knowledge-postgres-live` values, requires `database-url` credentials/database to match the individual Secret keys, and evaluates applied NetworkPolicy unions for Gateway, Console Plugin, Knowledge Engine, and Postgres. The PBS companion branch now includes `deploy/openshift-cywell-v014` to render the real PBS stack into namespace `playbookstudio` with a `playbookstudio-runtime` Service and the required runtime labels.
- Customer ACL concrete validation now evaluates `owners`, `users`, and `groups`; pbs-live rejects `default`, wildcard or prefix/suffix wildcard entries, string ACL values including non-wildcard string grants, non-string customer entries, placeholder principals/customers, and broad Kubernetes system groups before cutover.
- PBS live local/cluster smoke now checks valid-owner unmapped-customer `403` fail-closed behavior with no PBS trace, rejects conflicting nested customer metadata before PBS, and requires exact upload document/customer/source lineage through RAG citations, LLM Wiki notes, and direct topology doc-note edges.
- `render:pbs:live-prereqs` now requires `CAS_PBS_LIVE_PREREQS_OUT_DIR` or `--out-dir` pointing to an approved secure handoff path outside the repository before writing real live Secret manifests. It still records redacted evidence under `test-results/cas-pbs-live-prereqs-render.json`, rejects repo-local and repo-root raw Secret output unless the disposable lab override is explicit, renders `cas-knowledge-internal-auth/owner-hmac-secret`, rejects weak token/HMAC material, wildcard/default/string wildcard/broad-principal/non-string customer ACL policy, and mismatched Postgres `database-url`.
- `render:pbs:live-prereqs:template` writes non-secret handoff templates under ignored `test-results/pbs-live-prereqs-input-template/` so operators can copy them outside the repo and fill approved token, owner-HMAC, ACL, and Postgres inputs from a secret manager.
- `verify:pbs:live-prereqs` now writes self-test evidence separately under `test-results/cas-pbs-live-prereqs-self-test.json`; the release cutover bundle accepts only real-render evidence whose recorded Secret/ConfigMap files are hash-bound under the recorded output directory, whose generated-site ACL files live under that output directory's `pbs-live-site`, and whose rendered site overlay and redacted summary hashes still match the current files.
- `verify:pbs:source-contract` writes optional source/API evidence to `test-results/cas-pbs-source-contract.json`; `verify:pbs:source-contract:required` writes required-check evidence to `test-results/cas-pbs-source-contract-required.json`; `verify:release:source-pinning` writes strict clean/full-SHA evidence to `test-results/cas-pbs-source-contract-pinned.json` only after `CAS_PBS_SOURCE_HEAD` is supplied, the PBS checkout is clean, the PBS remote matches the approved GitHub host/repository, `git fetch --prune origin` succeeds, and at least one fetched `origin/*` branch contains the pinned SHA. The cutover bundle accepts only the pinned evidence file.
- Current strict source pin evidence passes for PBS SHA `6604777abb9e6bd44a83c6a12f36e31ac396489e` from `F:\AI_Projects\PBS-Dev3-cywell-v014-source-pin-clone`, branch `kugnus/cywell-v0.1.4-source-contract`, with clean PBS source and approved remote containment proof. This removes the PBS source-pin blocker for the current v0.1.4 evidence set, but the proof must still be rerun close to live cutover if freshness windows expire.
- `verify:release` aliases the non-skipping live release gate. `verify:release:pbs-live` runs strict source pinning, generated-site live preapply, applied live-site preflight, in-cluster cutover smoke, then renders the `--require-live-ready` cutover bundle. The bundle rejects dirty local evidence, dirty or unpinned PBS source evidence, unapproved PBS source remote identity, failed or stale remote-fetch source proof, pinned SHAs not contained in fetched approved remote refs, local-only Cywell release heads not contained in fetched approved `origin/*` refs, stale/missing HEAD evidence, stale preapply evidence, missing/failed cluster cutover smoke evidence, runtime pods not stamped with the pinned PBS source SHA, weak source contract hash sets, and self-test prerequisite evidence used as cutover proof.
- CRC deployment and release evidence now records full git HEAD, clean/dirty source status, and cluster identity; deploy refuses dirty tracked source by default, release promotion rejects wrong-HEAD, dirty-source, wrong-cluster, stale-evidence override, unpushed/local-only Cywell HEAD, or missing remote proof evidence, and strict live preflight compares release evidence cluster identity plus Cywell source proof when present.
- Customer ID changes in the console Knowledge route clear selected corpus documents, viewer state, action results, topology state, and document query params before the next RAG/Wiki/topology request, increment async scope revision, and invalidate in-flight topology requests so late upload/RAG/wiki/topology responses cannot restore an old customer scope.
- Knowledge route navigation preserves the active `customer_id` and selected `document_id` query context when moving between customer data, RAG, LLM Wiki, and topology pages; Wiki-note deep links now keep `document_id` lineage when a source document exists, bare invalid `document_id` query links are repaired to a real customer corpus document before RAG/wiki actions, empty-report invalid document links are cleared to full-corpus mode, and browser back/forward reconciles URL query state back into React state.
- RAG/wiki action callbacks read the latest selected-document scope at click time and ignore late selected-document responses after operators switch to full-corpus or another document scope.
- PBS preflight evidence names now include the generated site overlay identity, so `verify:pbs:preflight:live:site:preapply` writes `test-results/cas-pbs-preflight-pbs-live-site-preapply-cluster-required-secrets.json` instead of overwriting default pbs-live evidence.
- The cutover bundle now requires generated-site live preapply evidence with current clean `fullHead`, exact overlay path derived from the live prereq evidence output directory, `--require-cluster`, `--require-secret`, `--skip-applied`, freshness ordering after release/prereq evidence, and runtime pod source revision evidence; `--require-live-ready` additionally requires `test-results/cas-pbs-live-smoke-cluster-cutover.json` from write-enabled `verify-pbs-live-smoke.mjs --cutover --cluster` with `PASS pbs-live:cluster-write-lineage`. It also requires every required local evidence artifact to come from a clean tree, cluster identity to match CRC deployment/release/preapply/smoke evidence, recomputes the current generated live-prereq file/site-overlay/redacted-summary hashes, requires strict full-SHA PBS source pinning with approved remote identity, successful fresh remote fetch, approved remote ref containment proof, and exact contract-file hashes, and reports external live blockers in active blockers even when local evidence is invalid.
- The tracked pbs-live overlay now fails closed with `customer-access-json=__GENERATED_PBS_LIVE_SITE_OVERLAY_REQUIRED__`; production customer/group mapping must come from the reviewed generated `pbs-live-site` overlay.
- `deploy:pbs:runtime:crc` bootstraps the approved PBS runtime overlay in CRC, builds or reuses only a local PBS runtime image labeled with the pinned PBS SHA, applies/stamps the real `playbookstudio-runtime` deployment with the pinned PBS SHA, records the explicit CRC source-overlay hotfix ID, replays PBS one-shot jobs in order after stable Postgres readiness, verifies every migration/seed Job and ready runtime pod uses the source-bound local image digest, applies the pinned graph foundation migration, verifies strict DB/pgvector/corpus/compiled-wiki readiness, verifies the runtime Service/endpoint/ready pod SCC contract, and writes redacted deployment evidence to `test-results/cas-pbs-runtime-crc-deployment.json`.
- Strict live preflight now reads the actual PBS runtime readiness shape from `/api/health` (`runtime.db_corpus`) and `/api/wiki-loop/status?user_id=local`, binds cluster Secret values to the approved `render:pbs:live-prereqs` redacted hashes, passes live PBS/Postgres probe secrets through stdin instead of process argv, and rejects future-dated Cywell release remote proof.
- Strict live preflight now trusts the OpenShift service CA bundle mounted in PBS runtime pods when probing `playbookstudio-runtime.playbookstudio.svc.cluster.local:8765`, so service-ca signed HTTPS runtime health is verified without disabling TLS.
- CRC deploy now removes both `lightspeed-console-plugin` and legacy `cywell-opslens` from `console.operator.openshift.io/cluster.spec.plugins`, keeps `cywell-ai-sentinel` enabled, and disables the native `LightspeedButton`; the current CRC console operator was patched to `["networking-console-plugin","monitoring-plugin","cywell-ai-sentinel"]` and `deployment/console` rolled out successfully.
- In the current shell, `npm run render:pbs:live-prereqs` real rendering is blocked until approved non-placeholder live Secret material and ACL/DB inputs are provided. The renderer self-test still passes through `npm run verify:pbs:live-prereqs`.

Clean checkout reproduction:

```text
C:\Users\soulu\AppData\Local\Temp\cywell-v014-clean-20260629100550
npm ci: PASS
npm run verify: PASS
```

## CRC Evidence Policy

| Command | Result | Notes |
| --- | --- | --- |
| `npm run deploy:crc` | PASS, latest 72 runtime checks from clean pushed HEAD `2b2758c` | Mutating deploy action; rebuilds and deploys `cas-gateway:dev`, `cas-console-plugin:dev`, and `cas-knowledge-engine:dev` to CRC, creates/preserves local Secrets, applies CRC-only API/Lightspeed policies, disables native `lightspeed-console-plugin` plus legacy `cywell-opslens`, enables only the `cywell-ai-sentinel` launcher, verifies app pod imageID digests against dev ImageStreamTags, verifies PBS-compatible Postgres schema/shadow rows, and writes current `head` plus `verifiedImages` to `test-results/cas-crc-deployment.json` |
| `CAS_RELEASE_FORCE=true npm run release:crc:v0.1.4` | PASS, 21 checks from clean pushed HEAD `2b2758c` | Mutating release action; refuses stale/wrong-HEAD, dirty-source, wrong-cluster, stale-evidence override, or local-only Cywell HEAD evidence, verifies app release sources against runtime evidence, records `promotedImages` and Cywell remote proof, and force-promotes `cas-gateway`, `cas-console-plugin`, `cas-knowledge-engine`, and digest-pinned `cas-knowledge-postgres` to local `v0.1.4` ImageStreamTags |
| `npm run verify:pbs:preflight:shadow` | PASS/WARN | Render/config checks pass for `pbs-shadow`; current CRC warns because external `playbookstudio` namespace/service and optional `cas-pbs-auth` are absent |

Current CRC `v0.1.4` release image references are intentionally not duplicated in this tracked document. The source of truth is ignored evidence under `test-results/cas-release-images.json`, specifically `branch`, `head`, `status`, and `promotedImages`.

Local JSON evidence is written under ignored `test-results/`. Some diagnostic artifacts are intentionally stale or non-live-ready; `test-results/pbs-cutover-bundle/cutover-bundle.json` is the canonical current handoff gate that rejects stale, dirty, cross-cluster, unapproved-remote, or unpinned evidence before live-ready claims. The renderer also writes `test-results/cas-pbs-cutover-bundle.json` as a compatibility copy for older handoff paths. Strict pinned source evidence is required before live cutover, is generated only by `verify:release:source-pinning`, and currently passes for the approved PBS SHA above.

```text
test-results/cas-crc-connection.json
test-results/cas-crc-deployment.json
test-results/cas-deploy-manifests.json
test-results/cas-release-images.json
test-results/cas-pbs-live-smoke-local-cutover.json
test-results/cas-pbs-preflight-pbs-shadow-diagnostic-local-optional-secrets.json
test-results/cas-pbs-preflight-pbs-live-site-preapply-cluster-required-secrets.json
test-results/cas-pbs-live-prereqs-render.json (real-render evidence; regenerate at the current clean HEAD with approved live inputs before using as handoff evidence)
test-results/cas-pbs-live-prereqs-self-test.json
test-results/cas-pbs-source-contract.json (optional diagnostic only; live cutover accepts only cas-pbs-source-contract-pinned.json)
test-results/cas-pbs-source-contract-required.json
test-results/cas-pbs-source-contract-pinned.json (required for live cutover; generated by strict source pinning)
test-results/pbs-cutover-bundle/cutover-bundle.json
test-results/cas-pbs-cutover-bundle.json (compatibility copy)
```

## Expected Failing Live Cutover Gates

| Command | Result | Blocking Reason |
| --- | --- | --- |
| `npm run verify:pbs:cutover` | FAIL expected | `CAS_PBS_BASE_URL` is not configured in the local shell |
| `npm run verify:pbs:preflight:live:site:preapply` | FAIL expected, 56 PASS / 6 FAIL until live prerequisites are supplied | Generated site overlay render/config/egress/API checks pass, including parsed rendered NetworkPolicy shape checks; service-token transport is HTTPS, pbs-live Gateway customer ACL is wired to the generated live ConfigMap replacement, pbs-live Postgres image pinning passes, strict PBS source pin evidence passes, release ImageStreamTags resolve to digest-pinned references and match current release evidence, and the CRC `playbookstudio-runtime` Service passes endpoint, source revision, and Service-DNS `/api/health` readiness through the OpenShift service CA. Remaining blockers are missing `cas-pbs-auth`, missing `cas-knowledge-postgres-live`, missing real live-prereq render/hash binding, and the legacy CRC `cas-knowledge-postgres` dev Secret that must be pruned before pbs-live cutover |
| `npm run render:pbs:cutover-bundle` | FAIL expected until live prerequisite evidence is regenerated | Current handoff bundle must be `PASS/live-preapply-ready` for preapply readiness and `PASS` with `--require-live-ready` only after cluster cutover smoke. CRC deploy evidence, release image evidence, manifest evidence, and strict PBS source pin evidence are current at clean HEAD `2b2758c`. The bundle remains `FAIL/local-evidence-invalid` because `cas-pbs-live-prereqs-render.json` is still old-head `input-validation` FAIL evidence without approved live Secret/ACL/Postgres inputs; real live prereq render evidence must be regenerated from the current clean HEAD to a secure `CAS_PBS_LIVE_PREREQS_OUT_DIR`, generated-site preapply evidence must be fresh and hash-bound to that render, and live-ready bundling must include `cas-pbs-live-smoke-cluster-cutover.json`. External live blockers are reported both as external preapply blockers and active blockers |
| `npm run verify:release` / `npm run verify:release:pbs-live` | FAIL expected | Current strict PBS source pin evidence exists, but the live release gate still requires fresh proof at execution time, expected live cluster identity, secure live prerequisites, applied pbs-live workload cutover, in-cluster Gateway/console-plugin smoke, and the final live-ready cutover bundle |

Diagnostic non-ready states:

| Command | Result | Meaning |
| --- | --- | --- |
| `npm run verify:pbs:preflight` | exit 0 with WARNs possible | Defaults to pbs-live render/config diagnostics unless strict flags and live prerequisites are present |
| `npm run verify:pbs:preflight:shadow` | exit 0 with WARNs possible | Explicit pbs-shadow render/config diagnostics for the shadow apply phase |
| `npm run verify:pbs:preflight:shadow:cluster` | nonzero until cluster prerequisites exist | Strict pbs-shadow applied-cluster acceptance gate |
| `npm run verify:pbs:live` | exit 0 SKIP possible | Skips when `CAS_PBS_BASE_URL` is unset; not release readiness |

## Release Boundary

CRC v0.1.4 dev deployment is verified, PBS-compatible local shadow storage is exercised, clean companion PBS source/API contract shape is verified, strict PBS source pin proof exists for the clean pushed PBS branch `kugnus/cywell-v0.1.4-source-contract`, and local `v0.1.4` release ImageStreamTags are bound to verified CRC runtime digest evidence. Production PBS live cutover is not complete until the external HTTPS/mTLS PBS runtime, live Secrets, live overlay, PBS egress policy, live DB credential rotation/fresh PVC decision, expected cluster identity, runtime/corpus readiness, and `npm run verify:release` success are present.

The current CRC cluster already passes the Gateway Kubernetes API egress check for SelfSubjectReview/OpenShift evidence through the CRC overlay. Non-CRC live clusters still need their own cluster-specific Kubernetes API egress, because standard Kubernetes NetworkPolicy cannot allow `kubernetes.default.svc` by Service name.

The tracked pbs-live overlay fails closed with the sentinel `__GENERATED_PBS_LIVE_SITE_OVERLAY_REQUIRED__` in `cas-knowledge-live-config/customer-access-json`. For production, set `CAS_PBS_LIVE_PREREQS_OUT_DIR` to an approved secure handoff directory, run `render:pbs:live-prereqs`, and use that directory's generated `pbs-live-site` overlay so strict preflight renders the reviewed concrete customer/group mapping.

Operational checklist:

```text
deliverables/active/v0.1.4/production-live-cutover-checklist.md
```
