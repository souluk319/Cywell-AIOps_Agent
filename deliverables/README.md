# Deliverables Index

현재 브랜치에서 추적하는 산출물의 기준점은 아래 인덱스다.

```text
deliverables/active/README.md
```

현재 구현/검증 산출물:

```text
deliverables/active/v0.1.4/
```

v0.1.4 범위는 PBS 고객 데이터 업로드, RAG, LLM Wiki, topology dashboard, CRC runtime 검증, PBS live cutover gate와 production cutover checklist다.

장기 원본 자료와 foundation 문서는 로컬 작업 참고로 둘 수 있지만, 현재 브랜치에는 `deliverables/active/v0.1.4/**`와 active 인덱스만 릴리스 산출물로 추적한다.
